const names:  string[] = [];
names.push("Dylan"); 
// if we use readonly then we can not modify original array
// names.push(50); this line gives error as we declared above as string
console.log(names)